# Final Article Readiness Pass

* **Article**: `article/main.tex`
* **Timestamp**: 2026-05-26 18:07:21 -03
* **Scope**: GitHub issues #2--#9 from the camera-ready article PRD.

## Summary

The article is ready for another human review pass. The proof, narrative, terminology, worked example, citation integration, and final build checks are internally consistent. The article presents only the small worked example, not the larger internal numerical experiments.

## Checks Performed

### Build

`./scripts/compile_article.sh` passed. The script runs the full `pdflatex -> bibtex -> pdflatex -> pdflatex` cycle and fails on LaTeX warnings, BibTeX warnings, unresolved citations, overfull/underfull boxes, TODO markers, and compile errors.

### Citations

The article cites the following keys, all of which resolve through `article/references.bib`, a symlink to `../references/references.bib`:

`abud2018k`, `abud2024impossible`, `cassioli2015discretization`, `goncalves2021new`, `lavor2012discretizable`, `lavor2021optimality`, `liberti2008branch`, `liberti2011number`, `liberti2014number`, `mucherino2012discretizable`, `mucherino2012exploiting`, `mucherino2020analysis`.

The citation keys follow the agreed Google-Scholar-style pattern: first author, year, and a short title word.

### Literature Artifacts

The repository contains the human-in-the-loop Scopus search plan, Scopus CSV exports, screening batches, per-batch screening reports, shortlist, paper notes, citation reports, and integrated BibTeX entries. No article citation was added from title/abstract evidence alone. The user provided Scopus exports only; Web of Science queries remain available in the search plan but were not used in this pass.

### Terminology

Article-facing prose uses the canonical terms `cone generator`, `base generator`, `labeled violation`, `feasible branch shift space`, and `generic mirror separation`. The obsolete forms `dependency-cone generator` and `base-clique closure generator` do not appear as article-facing canonical terms. The glossary distinguishes `dependency cone` as a graph set from `cone generator` as the generator built from it.

### Worked Example

The six-vertex example is internally consistent:

* `L_P = {v_1, v_3, v_4, v_5, v_6}` and `B_P = {v_4, v_5, v_6}` follow from the stated predecessor and pruning data.
* The cone and base generator supports match the displayed mask matrix.
* The labeled violation rows match the two nonzero labels `({v_4,v_5},C_1)` and `({v_3,v_6},C_2)`.
* The ranks are over `\mathbb{F}_2`: `rank([M;V]) = 3` and `rank(V) = 2`.
* The final count is `|Xi_F| = 2`, and the coordinate realization verifies the nontrivial feasible branch shift.

## Remaining Reviewer-Facing Risks

1. Some bibliography entries outside the currently cited core set still have sparse metadata. Before journal submission, verify publisher metadata for all included but uncited entries, especially if the bibliography is expanded.
2. Web of Science exports were not incorporated because only Scopus exports were provided. If the venue expects a broader literature-search audit trail, run the planned Web of Science searches and add their screening report.
3. The article states generic mirror separation as the operative genericity assumption. If reviewers request a fuller algebraic-geometry justification of the exceptional set, add an appendix proof sketch rather than expanding the main theorem section.
4. `article/main.pdf` is generated locally and currently untracked. Track or package the PDF only when assembling the submission bundle.
